# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project Status

This project is in initial setup. No source code, dependencies, or tooling have been added yet. The repository currently contains only this file and a README.md.

## Project Purpose

`Claude-forex-agent` is intended to be an AI-powered agent for foreign exchange (forex) operations, built around Anthropic's Claude models.

## Getting Started

Before any development commands exist, the first step is to establish the tech stack (e.g., Python with `uv`/`pip`, Node.js with `npm`/`pnpm`, etc.) and commit the foundational configuration files (`pyproject.toml` or `package.json`, `.env.example`, `.gitignore`).

Once a stack is chosen, update this file with:
- Build, lint, and test commands
- How to run the agent locally
- Environment variable requirements
- High-level architecture description
